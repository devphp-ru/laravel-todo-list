class Toast {
    constructor(t) {
        this._title = !1 !== t.title && (t.title || "Title"), this._text = t.text || "Message...", this._theme = t.theme || "default", this._autohide = t.autohide && !0, this._interval = +t.interval || 5e3, this._create(), this._el.addEventListener("click", t => {
            t.target.classList.contains("toast__close") && this._hide()
        }), this._show()
    }

    _show() {
        this._el.classList.add("an_toast_showing"), this._el.classList.add("an_toast_show"), window.setTimeout(() => {
            this._el.classList.remove("an_toast_showing")
        }), this._autohide && setTimeout(() => {
            this._hide()
        }, this._interval)
    }

    _hide() {
        this._el.classList.add("an_toast_showing"), this._el.addEventListener("transitionend", () => {
            this._el.classList.remove("an_toast_showing"), this._el.classList.remove("an_toast_show"), this._el.remove()
        }, {once: !0});
        const t = new CustomEvent("hide.an_toast", {detail: {target: this._el}});
        document.dispatchEvent(t)
    }

    _create() {
        const t = document.createElement("div");
        t.classList.add("an_toast"), t.classList.add(`an_toast_${this._theme}`);
        let e = '{header}<div class="an_toast__body"></div><button class="an_toast__close" type="button"></button>';
        const s = !1 === this._title ? "" : '<div class="an_toast__header"></div>';
        if (e = e.replace("{header}", s), t.innerHTML = e, this._title ? t.querySelector(".an_toast__header").textContent = this._title : t.classList.add("an_toast_message"), t.querySelector(".an_toast__body").textContent = this._text, this._el = t, !document.querySelector(".an_toast-container")) {
            const t = document.createElement("div");
            t.classList.add("an_toast-container"), document.body.append(t)
        }
        document.querySelector(".an_toast-container").append(this._el)
    }
}
